many particle gravity simulator 

Run via
```
g++ -std=c++11 -O3 -fopenmp <FILE>
```

### TODO
* Remove printParaviewSnapshot() at end of sim call if plotting is off 
* Fix plots in step 2 report
